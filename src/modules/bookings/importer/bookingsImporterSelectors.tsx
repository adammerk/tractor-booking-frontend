import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const bookingsImporterSelectors = importerSelectors(
  'bookings.importer',
);

export default bookingsImporterSelectors;
