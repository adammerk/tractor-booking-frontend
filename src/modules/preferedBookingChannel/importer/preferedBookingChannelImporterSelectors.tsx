import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const preferedBookingChannelImporterSelectors = importerSelectors(
  'preferedBookingChannel.importer',
);

export default preferedBookingChannelImporterSelectors;
